aiogram>=3.0.0
python-dotenv>=1.0.0
aiosqlite>=0.19.0
aiofiles>=23.0.0
asyncpg>=0.28.0
aiomysql>=0.2.0
aiohttp>=3.9.0

# 加密（可选，若未安装则使用内置回退实现）
cryptography>=41.0.0

# 备份
schedule>=1.2.0

# 测试
pytest>=7.4.0
pytest-asyncio>=0.21.0
